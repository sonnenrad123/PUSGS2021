import { WorkRequestAttachments } from './work-request-attachments';

describe('WorkRequestAttachments', () => {
  it('should create an instance', () => {
    expect(new WorkRequestAttachments()).toBeTruthy();
  });
});
