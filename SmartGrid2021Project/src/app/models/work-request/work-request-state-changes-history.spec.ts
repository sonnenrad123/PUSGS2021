import { WorkRequestStateChangesHistory } from './work-request-state-changes-history';

describe('WorkRequestStateChangesHistory', () => {
  it('should create an instance', () => {
    expect(new WorkRequestStateChangesHistory()).toBeTruthy();
  });
});
