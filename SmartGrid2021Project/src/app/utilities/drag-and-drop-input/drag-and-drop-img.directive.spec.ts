import { DragAndDropImgDirective } from './drag-and-drop-img.directive';

describe('DragAndDropImgDirective', () => {
  it('should create an instance', () => {
    const directive = new DragAndDropImgDirective();
    expect(directive).toBeTruthy();
  });
});
